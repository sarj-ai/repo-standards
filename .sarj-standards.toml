# Managed by `sarj-standards setup`; commit this file.
schema = 3
bundle = "5.2.0"
profile = "application"
rule_profile = "all"

[capabilities]
disable = ["eslint"]

[artifacts]
durable = [".github/**"]

[dest]
python = "."
typescript = "."

[hooks]
manager = "pre-commit"
