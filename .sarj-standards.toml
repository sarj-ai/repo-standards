# Managed by `code-standards setup`; commit this file.
schema = 4
bundle = "7.14.23"
rule_profile = "all"

[capabilities]
disable = ["eslint", "swiftformat", "swiftlint", "ktlint", "detekt", "mobile-security"]

[artifacts]
durable = [".github/**"]

[dest]
python = "."
typescript = "apps/docs"
swift = "."
kotlin = "."

[hooks]
manager = "pre-commit"
