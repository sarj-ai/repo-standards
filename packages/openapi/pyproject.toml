[project]
name = "repo-lint-openapi"
version = "0.1.0"
description = "Deterministic, read-only OpenAPI evidence and objective policy rules"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling==1.27.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/repo_lint_openapi"]
