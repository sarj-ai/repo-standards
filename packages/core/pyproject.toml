[project]
name = "repo-lint-core"
version = "0.1.0"
description = "Company-neutral repository lint engine"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling==1.27.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/repo_lint_core"]
